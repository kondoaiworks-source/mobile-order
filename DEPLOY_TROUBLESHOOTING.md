# 本番環境の変更が反映されない場合の対処法

## 変更が反映されない原因

本番環境にデプロイしても変更が見られない場合、以下の原因が考えられます：

1. **ブラウザのキャッシュ**
2. **デプロイがまだ完了していない**
3. **Vercelのデプロイが失敗している**

## 確認・対処方法

### 1. Vercelダッシュボードでデプロイ状況を確認

1. **Vercelダッシュボードにアクセス**
   - https://vercel.com/dashboard
   - プロジェクトを選択

2. **デプロイメントの確認**
   - 「Deployments」タブを開く
   - 最新のデプロイメントを確認
   - ステータスが「Ready」になっているか確認

3. **デプロイログの確認**
   - 最新のデプロイメントをクリック
   - 「Build Logs」または「Function Logs」を確認
   - エラーがないか確認

### 2. ブラウザのキャッシュを完全にクリア

#### Chrome / Edge（デスクトップ）
1. `Cmd+Shift+Delete` (Mac) または `Ctrl+Shift+Delete` (Windows)
2. 「キャッシュされた画像とファイル」を選択
3. 「時間の範囲」で「全期間」を選択
4. 「データを削除」をクリック

#### 強制リロード（最も簡単）
- **Mac**: `Cmd + Shift + R`
- **Windows/Linux**: `Ctrl + Shift + R`
- または `Ctrl + F5`

#### シークレットモード（プライベートモード）で確認
- ブラウザをシークレットモード（プライベートモード）で開く
- 本番環境のURLにアクセス
- 変更が反映されているか確認

### 3. デプロイの再実行（必要に応じて）

Vercelダッシュボードで：
1. 最新のデプロイメントを選択
2. 「Redeploy」ボタンをクリック
3. デプロイが完了するまで待機（1-3分）

### 4. 変更内容の確認

実際に変更された内容を確認：

#### カテゴリページ（`/category/[category名]`）
- ✅ 左側に小さな画像（約96px × 96px）
- ✅ 右側にメニュー名、備考、価格、数量選択、カート追加ボタン
- ✅ 横型レイアウトになっている

#### 会計タブ（`/checkout`）
- ✅ 「会計する」ボタンを押すと、注文履歴の詳細が表示される
- ✅ 各メニュー項目の詳細（商品名、数量、価格）が表示される
- ✅ 「会計します」ボタンを押すと、「ありがとうございました。レジまでお越しください。」が表示される

### 5. ブラウザの開発者ツールで確認

1. **ブラウザの開発者ツールを開く**（F12 または右クリック→検証）
2. **ネットワークタブを開く**
3. **「キャッシュを無効にする」にチェックを入れる**
4. ページを再読み込み（F5）

### 6. 別のブラウザまたはデバイスで確認

- 異なるブラウザで確認
- スマートフォンで確認（別のデバイス）

## デプロイが失敗している場合

### エラーログの確認

Vercelダッシュボードで：
1. 失敗したデプロイメントを選択
2. 「Build Logs」を確認
3. エラーメッセージを確認

### よくあるエラー

#### ビルドエラー
- TypeScriptの型エラー
- 依存関係のエラー
- 環境変数の設定ミス

**対処法**:
```bash
# ローカルでビルドテスト
npm run build
```

#### 環境変数のエラー
- Supabaseの環境変数が設定されていない

**対処法**:
1. Vercelダッシュボード → Settings → Environment Variables
2. 環境変数が正しく設定されているか確認

## 変更が反映されているか確認する方法

### 1. ページソースを確認

1. ブラウザでページを開く
2. 右クリック → 「ページのソースを表示」
3. HTMLに最新の変更が含まれているか確認

### 2. JavaScriptファイルのハッシュを確認

1. ブラウザの開発者ツールを開く（F12）
2. 「Network」タブを開く
3. ページを再読み込み
4. JavaScriptファイル（`.js`）を確認
5. ファイル名に含まれるハッシュが最新か確認

### 3. バージョン情報を確認

コンソールで以下を実行：
```javascript
console.log(window.__NEXT_DATA__)
```

## それでも解決しない場合

1. **GitHubのコミット履歴を確認**
   - 変更が正しくコミットされているか
   - プッシュされているか

2. **Vercelの再デプロイ**
   - Vercelダッシュボードで手動で再デプロイ

3. **ローカル環境で確認**
   - ローカル環境で `npm run build` を実行
   - エラーがないか確認

4. **サポートに問い合わせ**
   - エラーメッセージやスクリーンショットを添付

---

**最も効果的な対処法**: ブラウザのキャッシュをクリアしてから、強制リロード（`Cmd+Shift+R` / `Ctrl+Shift+R`）してください。

