# ゲスト側に変更を反映させる方法

## 問題の状況

変更は本番環境にデプロイ済みですが、ゲスト側（ユーザー側）のブラウザで古いバージョンが表示される可能性があります。

## 原因

**ブラウザキャッシュ**が原因です。ブラウザが以前に表示したページをキャッシュに保存しているため、新しいバージョンが表示されません。

## ゲスト側への案内方法

ゲスト（ユーザー）に以下の方法を案内してください：

### 方法1: 強制リロード（最も簡単）

**iPhone/Android（スマートフォン）**:
1. ブラウザアプリを完全に閉じる
2. 再度ブラウザアプリを開く
3. 本番環境のURLにアクセス

**PC（デスクトップ）**:
- **Windows/Linux**: `Ctrl + Shift + R` または `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

### 方法2: キャッシュをクリア

#### iPhone (Safari)
1. 設定アプリを開く
2. Safari を選択
3. 「履歴とWebサイトデータを消去」をタップ
4. 確認をタップ

#### Android (Chrome)
1. Chromeアプリを開く
2. メニュー（三点） → 設定
3. プライバシーとセキュリティ
4. 「閲覧履歴データを削除」をタップ
5. 「キャッシュされた画像とファイル」にチェック
6. 「データを削除」をタップ

#### PC (Chrome/Edge)
1. `Ctrl+Shift+Delete` (Windows) または `Cmd+Shift+Delete` (Mac)
2. 「キャッシュされた画像とファイル」にチェック
3. 「時間の範囲」で「全期間」を選択
4. 「データを削除」をクリック

### 方法3: シークレットモード（プライベートモード）で確認

- ブラウザをシークレット/プライベートモードで開く
- 本番環境のURLにアクセス
- 変更が反映されているか確認

## 変更が反映されているか確認する方法

### カテゴリページ

1. カテゴリを選択
2. 以下の表示になっていれば成功：
   - ✅ 左側に小さな画像（約96px × 96px）
   - ✅ 右側にメニュー名、備考、価格、数量選択、カート追加ボタン
   - ✅ 横並びレイアウト

### 会計タブ

1. 会計タブにアクセス
2. 「会計する」ボタンをクリック
3. 以下の表示になっていれば成功：
   - ✅ 注文履歴の詳細（各メニュー項目の詳細）
   - ✅ 商品名、数量、価格が表示される
   - ✅ その下に合計金額が表示される
4. 「会計します」ボタンをクリック
5. 以下の表示になっていれば成功：
   - ✅ オーバーレイで「ありがとうございました。レジまでお越しください。」が表示される
   - ✅ 「。」で改行されている

## ゲスト側への案内文（テンプレート）

ゲストに以下のような案内をしてください：

```
【重要なお知らせ】

アプリを最新の状態でご利用いただくため、以下の操作をお願いいたします。

■ スマートフォンの場合
1. ブラウザアプリを一度完全に閉じる
2. 再度ブラウザアプリを開く
3. アプリにアクセスしてください

■ PCの場合
キーボードで「Ctrl + Shift + R」（Windows）または「Cmd + Shift + R」（Mac）を押してください

これで最新のバージョンが表示されます。
```

## 管理者側での確認方法

1. **Vercelダッシュボードでデプロイ状況を確認**
   - 最新のデプロイメントが「Ready」になっているか
   - デプロイが成功しているか

2. **シークレットモードで確認**
   - 管理者自身もシークレットモードで確認
   - 変更が反映されているか確認

3. **複数のデバイス/ブラウザで確認**
   - 異なるブラウザで確認
   - スマートフォンで確認

## 技術的な対策（管理者側）

Next.jsの設定を調整して、キャッシュヘッダーを設定しました。これにより、ブラウザが新しいバージョンを優先的に取得するようになります。

ただし、**ゲスト側のブラウザキャッシュをクリアするのは、ゲスト自身が行う必要があります**。

---

**重要**: 変更は既に本番環境にデプロイされています。ゲスト側でブラウザキャッシュをクリアしていただければ、最新のバージョンが表示されます。

