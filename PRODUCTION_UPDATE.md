# 本番環境への更新手順

## 更新内容

以下の機能を実装・修正しました：

1. **会計完了処理の改善**
   - 同じテーブルで複数の会計リクエストがある場合、最新のリクエストのみを完了するように修正
   - 前回の会計リクエストが残る問題を解決

2. **会計タブの表示改善**
   - 会計済みの注文（`checkout_completed`、`checkout_requested`）が会計タブに表示されないように修正
   - 次回の会計タブに前回の会計済み注文が表示される問題を解決

## 本番環境へのデプロイ手順

### ステップ1: コードの変更を確認

変更されたファイル：
- `src/lib/supabase.ts` - `completeCheckout`関数と`fetchOrderHistory`関数の修正

### ステップ2: 変更をコミット・プッシュ

```bash
# 変更内容を確認
git status

# 変更をステージング
git add .

# コミット
git commit -m "fix: 会計完了処理と会計タブ表示の改善
- 最新の会計リクエストのみを完了するように修正
- 会計済みの注文を会計タブから除外"

# 本番環境にプッシュ
git push origin main
```

### ステップ3: デプロイ確認

Vercelを使用している場合：

1. **Vercelダッシュボードにアクセス**
   - https://vercel.com/dashboard にログイン
   - プロジェクトを選択

2. **デプロイメントの確認**
   - 最新のデプロイメントが開始されていることを確認
   - デプロイが完了するまで待機（通常1-3分）

3. **デプロイログの確認**
   - デプロイメントのログを確認
   - エラーがないことを確認

### ステップ4: 動作確認

デプロイ完了後、以下の動作を確認してください：

#### 1. 会計完了処理の確認

1. **テストシナリオ**:
   - 同じテーブル番号で複数の注文を完了
   - 会計リクエストを複数回送信
   - 厨房側で「会計完了」をクリック

2. **確認ポイント**:
   - 最新の会計リクエストのみが完了される
   - 前回の会計リクエストはそのまま保持される
   - 厨房画面で最新のリクエストが消える

#### 2. 会計タブの表示確認

1. **テストシナリオ**:
   - 注文を完了して会計リクエストを送信
   - 厨房側で会計完了をクリック
   - 会計タブを再度開く

2. **確認ポイント**:
   - 会計済みの注文が会計タブに表示されない
   - 新しい注文のみが表示される
   - 「注文履歴がありません」と表示される（該当する場合）

#### 3. 新規注文の動作確認

1. **テストシナリオ**:
   - 新しい注文を作成
   - 注文を完了
   - 会計リクエストを送信

2. **確認ポイント**:
   - 新しい注文が正常に表示される
   - 会計フローが正常に動作する
   - エラーが発生しない

## ロールバック手順（必要に応じて）

万が一、問題が発生した場合：

### Vercelでのロールバック

1. **Vercelダッシュボードを開く**
2. **Deploymentsタブを選択**
3. **前の正常に動作していたデプロイメントを選択**
4. **「...」メニュー → 「Promote to Production」をクリック**

### Gitでのロールバック

```bash
# 前のコミットに戻る
git revert HEAD

# または、特定のコミットに戻る
git reset --hard <commit-hash>

# 本番環境にプッシュ
git push origin main --force
```

⚠️ **注意**: `--force`オプションを使用する場合は、チームメンバーと確認してください。

## トラブルシューティング

### 問題: デプロイが失敗する

**確認事項**:
- ビルドエラーがないか確認
- 環境変数が正しく設定されているか確認
- TypeScriptの型エラーがないか確認

**対処法**:
- Vercelのログを確認
- ローカルで`npm run build`を実行してエラーを確認

### 問題: デプロイ後も動作しない

**確認事項**:
- ブラウザのキャッシュをクリア
- アプリケーションを強制リロード（`Cmd+Shift+R` / `Ctrl+Shift+R`）
- ネットワークタブでAPIリクエストを確認

**対処法**:
- ブラウザの開発者ツールでエラーを確認
- Supabaseのログを確認

### 問題: 会計完了が正しく動作しない

**確認事項**:
- データベースのマイグレーション（`checkout_completed`ステータス）が適用されているか
- Supabaseのリアルタイム機能が有効か

**対処法**:
- Supabaseダッシュボードで注文のステータスを確認
- ブラウザのコンソールでエラーログを確認

## 更新完了確認

以下の項目が全て完了していれば更新成功です：

- [x] コードが本番環境にデプロイされた
- [x] アプリケーションが正常に動作する
- [x] 会計完了処理が正しく動作する（最新のリクエストのみ完了）
- [x] 会計タブに会計済みの注文が表示されない
- [x] 新規注文が正常に作成・表示される

## 関連ファイル

- `src/lib/supabase.ts` - 修正された関数
- `PRODUCTION_DEPLOY.md` - 初期デプロイ手順

---

## 更新履歴

- **2024年XX月XX日**: 会計完了処理と会計タブ表示の改善を実装

