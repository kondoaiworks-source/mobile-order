# テスト環境での確認方法（クイックガイド）

## 開発サーバーの起動

### 1. 依存関係のインストール（初回のみ）

```bash
npm install
```

### 2. 開発サーバーを起動

```bash
npm run dev
```

### 3. ブラウザでアクセス

開発サーバーが起動すると、以下のようなメッセージが表示されます：

```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Environments: .env.local

 ✓ Ready in X.XXs
```

ブラウザで **http://localhost:3000** にアクセスしてください。

### 4. 開発サーバーを停止

ターミナルで `Ctrl + C` を押してください。

## 環境変数の確認

`.env.local`ファイルが存在し、以下の環境変数が設定されていることを確認してください：

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

環境変数がない、または間違っている場合は、エラーメッセージが表示されます。

## テスト項目

開発環境で以下を確認してください：

1. **メインページ**
   - http://localhost:3000 にアクセス
   - 商品一覧が表示される

2. **カテゴリページ**
   - カテゴリをクリック
   - 横型レイアウト（左に画像、右に情報）で表示される
   - 上下スワイプでメニューを切り替えられる

3. **カート機能**
   - 商品をカートに追加
   - カート画面で確認

4. **会計タブ**
   - 会計タブにアクセス
   - 「会計する」ボタンで注文履歴の詳細が表示される
   - 「会計します」ボタンでメッセージが表示される

5. **厨房画面**
   - /kitchen にアクセス
   - 注文が表示される

## トラブルシューティング

### ポート3000が使用中

別のポートを使用する：

```bash
npm run dev -- -p 3001
```

### 環境変数エラー

`.env.local`ファイルを確認し、環境変数が正しく設定されているか確認してください。

### モジュールが見つからない

```bash
npm install
```

---

詳細は `TEST_ENVIRONMENT.md` を参照してください。

