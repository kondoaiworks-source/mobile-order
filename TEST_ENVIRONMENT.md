# テスト環境（開発環境）での確認方法

## 前提条件

1. **Node.jsがインストールされていること**
   - Node.js 18以上を推奨
   - バージョン確認: `node --version`

2. **環境変数が設定されていること**
   - `.env.local`ファイルにSupabaseの認証情報が設定されている必要があります

## セットアップ手順

### 1. 依存関係のインストール

```bash
npm install
```

### 2. 環境変数の設定

`.env.local`ファイルをプロジェクトルートに作成し、以下の環境変数を設定してください：

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**環境変数の取得方法**:
1. Supabaseダッシュボードにログイン
2. プロジェクトを選択
3. **Settings** → **API** に移動
4. **Project URL** をコピー → `NEXT_PUBLIC_SUPABASE_URL`に設定
5. **anon public**キー（`eyJ`で始まる）をコピー → `NEXT_PUBLIC_SUPABASE_ANON_KEY`に設定

**⚠️ 重要**: 
- `anon public`キーを使用してください（`sb_secret_`で始まるシークレットキーは使用しないでください）
- `.env.local`ファイルはGitにコミットされません（`.gitignore`に含まれています）

### 3. 開発サーバーの起動

```bash
npm run dev
```

### 4. ブラウザでアクセス

開発サーバーが起動すると、以下のメッセージが表示されます：

```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Environments: .env.local

 ✓ Ready in X.XXs
```

ブラウザで **http://localhost:3000** にアクセスしてください。

## 開発サーバーのコマンド

### 開発サーバーの起動

```bash
npm run dev
```

### 開発サーバーの停止

ターミナルで `Ctrl + C` を押してください。

### 本番ビルドのテスト

```bash
npm run build
npm start
```

本番環境と同様の動作をローカルでテストできます。

## よくある問題と対処法

### 問題1: 環境変数エラー

**エラーメッセージ**: `Supabaseの環境変数が設定されていません`

**対処法**:
1. `.env.local`ファイルがプロジェクトルートに存在するか確認
2. 環境変数の名前が正しいか確認（`NEXT_PUBLIC_`プレフィックスが必要）
3. 環境変数の値が正しく設定されているか確認
4. 開発サーバーを再起動（`Ctrl + C`で停止 → `npm run dev`で再起動）

### 問題2: ポート3000が使用中

**エラーメッセージ**: `Port 3000 is already in use`

**対処法**:
1. 別のポートを使用する:
   ```bash
   npm run dev -- -p 3001
   ```
2. または、ポート3000を使用しているプロセスを終了する

### 問題3: モジュールが見つからない

**エラーメッセージ**: `Cannot find module 'xxx'`

**対処法**:
```bash
npm install
```

### 問題4: データベース接続エラー

**対処法**:
1. Supabaseダッシュボードでプロジェクトがアクティブか確認
2. 環境変数の値が正しいか確認
3. ネットワーク接続を確認

## 開発時のヒント

### ホットリロード

ファイルを保存すると、自動的にブラウザがリロードされます（Hot Module Replacement）。

### コンソールログの確認

ブラウザの開発者ツール（F12）→コンソールタブで、デバッグログやエラーメッセージを確認できます。

### ネットワークタブ

ブラウザの開発者ツール→ネットワークタブで、SupabaseへのAPIリクエストを確認できます。

## テスト項目

開発環境で以下を確認してください：

- [ ] メインページが表示される
- [ ] 商品が正しく表示される
- [ ] カテゴリページでメニューが表示される（横型レイアウト）
- [ ] カートに商品を追加できる
- [ ] 注文を送信できる
- [ ] 会計タブで注文履歴が表示される
- [ ] 「会計する」ボタンで詳細が表示される
- [ ] 「会計します」ボタンでメッセージが表示される
- [ ] 厨房画面で注文が表示される

## 関連ファイル

- `README.md` - 基本的なセットアップ手順
- `DEPLOYMENT.md` - 本番環境へのデプロイ手順

---

**開発環境で動作確認後、本番環境にデプロイしてください！**

